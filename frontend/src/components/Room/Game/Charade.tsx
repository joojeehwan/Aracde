import React, { useCallback, useEffect, useRef, useState } from 'react';

const Charade = () => {
  const [second, setSecond] = useState<any>(60);
  const [minute, setMinute] = useState<any>(0);
  return (
    <>
      <div>
        <h1>안녕하세요</h1>
      </div>
    </>
  );
};

export default Charade;
